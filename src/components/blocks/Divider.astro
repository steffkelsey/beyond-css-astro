---
import Divider from '../Divider.astro';
import type { DividerBlock } from '../../lib/tina/data';

interface Props {
  data: DividerBlock;
}
const { data } = Astro.props;
---

<div class:list={['container', data.withPadding && 'section']}>
  <Divider />
</div>
